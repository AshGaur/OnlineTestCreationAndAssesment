schema{
	query: Query
}

type Query{
	allAdmins: [Admin]
	admin(id:Int): Admin
}

type Admin{
	id: Int
	name:String
	user: User
}

type User{
	email:String
	password:String
	roles:String
	phone: String
	active:Boolean
}